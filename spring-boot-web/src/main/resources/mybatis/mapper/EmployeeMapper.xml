<!--<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sunwx.springboot.mapper.employeeMapper">
    <select id="selectById" resultType="com.sunwx.springboot.bean.Employee">
    select * from employee where id = #{id}
  </select>
    <insert id="insertEmp" >
        insert into employee(name,email,tel_phone) values (#{name},#{email},#{tel_name})
    </insert>
</mapper>-->